/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['m.media-amazon.com','i.postimg.cc','randomuser.me'], // Add allowed domains here
    },
  };

export default nextConfig;
